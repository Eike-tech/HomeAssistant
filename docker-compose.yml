services:
  dashboard:
    build:
      context: .
      args:
        NEXT_PUBLIC_HASS_URL: ${NEXT_PUBLIC_HASS_URL}
        NEXT_PUBLIC_HASS_TOKEN: ${NEXT_PUBLIC_HASS_TOKEN}
    ports:
      - "3000:3000"
    restart: unless-stopped
